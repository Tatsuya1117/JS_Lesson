let name = 'Tim';

function hello(name) {
    console.log('hello ' + name);
}

hello(name);

let obj = {
    property1: 'Hello',
    property2: function() {
    },
    property3: {
        d: 'Bye'
    }
}